/**
 * Post CSS configuration for compiling Tailwind CSS into normal CSS.
 * @type {import('postcss-load-config').Config} The configuration type.
 */
export default {
	plugins: {
		tailwindcss: {}
	}
}
